# Local Seeds

`clean` removes deterministic Phase 1 data and creates the local SENTINELA master user.

`demo` inserts small visual demo records prefixed with `phase1-demo`. It is optional and can be removed by running the clean seed again.

Local SENTINELA login:

- Email: `local.master@supreme.local`
- Password: `supreme-local-admin`
