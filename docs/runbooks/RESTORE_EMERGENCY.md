# Runbook - Emergency Restore

Confirm incident commander approval, verify dump hash, test restore into a
temporary database, stop writers, run destructive restore, then run Phase 1 E2E
and custody checks.

