# Researcher Guide

Use SENTINELA exports for CSV, JSON, Parquet and data dictionary. Treat outputs
as analytical indicators, not diagnosis or automatic causal proof.

Verify `algorithm_version`, `algorithm_parameters` and scope before analysis.

