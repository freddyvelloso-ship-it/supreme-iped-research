# Operator Guide

Operate from healthchecks, SLO dashboards and runbooks. Do not reset data or run
destructive restore commands without incident approval.

Use `scripts/local.ps1 -Action health` for local validation and Prometheus/Grafana
for staging or production.

